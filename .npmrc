# pnpm settings for Echo.
#
# minimum-release-age=0:
#   pnpm 11+ rejects packages published within the last 24h by default. For a
#   solo project where we explicitly review every dep we add, this is more
#   friction than safety. Set to 0 so new deps install immediately on the
#   build server.
minimum-release-age=0
